// This is the gravity ball version 1 which will only draw a ball and a line at the bottom of the screen
// 21/11/2018
// Lyall Beveridge

void setup() {
  size(600, 600);      // Draw a square background
  
  // Draw a circle to begin with
  ellipseMode(CENTER);
  fill(255);
  ellipse(width/2, height/2, 50, 50);
}

void draw() {
 
}
